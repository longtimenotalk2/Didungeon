use egui::Ui;

use crate::game::unit::Unit;

pub fn show_unit(ui : &mut Ui, unit : &Unit) {
    ui.group(|ui| {
        ui.set_height(200.0);
        ui.set_width(123.6);

        // // 重要属性
        // ui.
    });
}